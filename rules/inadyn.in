## SECTION=staging
## old section:
### SECTION=networking
config INADYN
	tristate
	prompt "inadyn"
	help
	  Build the client for use with DynDNS and other services like this.
	  To configure this tool use your own projectroot/etc/inadyn.conf file.
	  You can use the generic one coming with ptxdist as a template.

	  STAGING: remove in PTXdist 2027.03.0
	  Upstream is gone, unmaintained in PTXist. Fails to build with gcc 15.
